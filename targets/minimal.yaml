target:
  name: demo
  type: api
  settings:
    response_selector: "response"
    request_body_template: '{"message": "{{prompt}}"}'

goal: "system_prompt_extraction"
strategies:
  - "directly_asking"
