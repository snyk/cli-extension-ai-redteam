target:
  name: "URL Command Config"
  type: http
  context:
    purpose: "Testing chatbot"
  settings:
    url_command:
      binary: "echo"
      args: ["https://session.example.com"]
    response_selector: "response"
    request_body_template: '{"message": "{{prompt}}"}'
goals:
  - "system_prompt_extraction"
